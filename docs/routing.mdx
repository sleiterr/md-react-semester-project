---\ntitle: Routing\ndescription: React Router setup og sider\n---\n\n# 🔗 Routing\n\n## Setup\n\nProjektet bruger **react-router-dom v6**.\n\n### App.jsx - Route Definition\n\n```jsx\nimport { BrowserRouter, Routes, Route } from 'react-router-dom'\nimport Home from './pages/Home/Home'\nimport ProductPage from './pages/ProductPage/ProductPage'\nimport ProductDetail from './pages/ProductDetail/ProductDetail'\nimport CartPage from './pages/CartPage/CartPage'\nimport Contact from './pages/Contact/Contact'\n\nfunction App() {\n  return (\n    <BrowserRouter>\n      <Routes>\n        <Route path=\"/\" element={<Home />} />\n        <Route path=\"/products\" element={<ProductPage />} />\n        <Route path=\"/products/:id\" element={<ProductDetail />} />\n        <Route path=\"/cart\" element={<CartPage />} />\n        <Route path=\"/contact\" element={<Contact />} />\n      </Routes>\n    </BrowserRouter>\n  )\n}\nexport default App\n```\n\n## Route Oversicht\n\n| Path | Komponent | Formål |\n|------|-----------|--------|\n| `/` | `Home.jsx` | Forside med hero + highlighted produkter |\n| `/products` | `ProductPage.jsx` | Alle produkter med filter & søgning |\n| `/products/:id` | `ProductDetail.jsx` | Enkelt produkt detaljer |\n| `/cart` | `CartPage.jsx` | Indkøbskurv & betaling |\n| `/contact` | `Contact.jsx` | Kontaktformular |\n\n## Navigation\n\n### Link Component\n```jsx\nimport { Link } from 'react-router-dom'\n\n// Til produktdetail\n<Link to={`/products/${product.id}`}>\n  Se detaljer\n</Link>\n\n// Til kurv\n<Link to=\"/cart\">\n  Gå til kurv\n</Link>\n```\n\n### useNavigate Hook\n```jsx\nimport { useNavigate } from 'react-router-dom'\n\nfunction ProductCard() {\n  const navigate = useNavigate()\n  \n  const handleViewDetails = (id) => {\n    navigate(`/products/${id}`)\n  }\n  \n  return (\n    <button onClick={() => handleViewDetails(5)}>\n      Se detaljer\n    </button>\n  )\n}\n```\n\n## URL Parameters\n\n### Læs product ID fra URL\n\n```jsx\nimport { useParams } from 'react-router-dom'\n\nfunction ProductDetail() {\n  const { id } = useParams()  // Hent :id fra URL\n  \n  // Fetch produktet baseret på ID\n  const product = products.find(p => p.id === parseInt(id))\n  \n  return <div>{product.title}</div>\n}\n```\n\n## Query Strings (Optional)\n\nHvis du vil gemme filter status i URL (good for bookmarking):\n\n```jsx\nimport { useSearchParams } from 'react-router-dom'\n\nfunction ProductPage() {\n  const [searchParams, setSearchParams] = useSearchParams()\n  \n  const category = searchParams.get('category')  // ?category=electronics\n  const sort = searchParams.get('sort')          // ?sort=price-asc\n  \n  const handleFilterChange = (newCategory) => {\n    setSearchParams({ category: newCategory })\n  }\n  \n  return (\n    <div>\n      {/* Filter controls */}\n      <button onClick={() => handleFilterChange('electronics')}>\n        Electronics\n      </button>\n    </div>\n  )\n}\n```\n\n**URL eksempel:**\n```\n/products?category=electronics&sort=price-asc\n```\n\n## Navigation fra Header\n\n```jsx\n// Header.jsx\nimport { Link } from 'react-router-dom'\n\nfunction Header() {\n  return (\n    <header className={styles.header}>\n      <Link to=\"/\" className={styles.logo}>MD Webshop</Link>\n      \n      <nav className={styles.nav}>\n        <Link to=\"/\">Hjem</Link>\n        <Link to=\"/products\">Produkter</Link>\n        <Link to=\"/contact\">Kontakt</Link>\n        <Link to=\"/cart\" className={styles.cart}>\n          🛒 Kurv (3)\n        </Link>\n      </nav>\n    </header>\n  )\n}\n```\n\n## Active Link Styling\n\n```jsx\nimport { NavLink } from 'react-router-dom'\n\n<NavLink \n  to=\"/products\" \n  className={({ isActive }) => isActive ? styles.active : ''}\n>\n  Produkter\n</NavLink>\n```\n\n## Error Handling (Optional)\n\nHvis produkt ikke findes:\n\n```jsx\nfunction ProductDetail() {\n  const { id } = useParams()\n  const product = products.find(p => p.id === parseInt(id))\n  \n  if (!product) {\n    return <div>Produktet blev ikke fundet</div>\n  }\n  \n  return <div>{product.title}</div>\n}\n```\n\n---\n\n**Næste:** Læs [Features - Produkter](/features-products) for filtrering & søgning\n"